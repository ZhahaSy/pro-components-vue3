import { withInstall } from '@vue-pro-components/utils';
// eslint-disable-next-line import/no-self-import
import Comp from './ediatableList';

export const EditableList = withInstall(Comp);

export default EditableList;
