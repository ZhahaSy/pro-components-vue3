import { withInstall } from '@vue-pro-components/utils';
import ActionDialogComp from './ActionDialog';

export const ActionDialog = withInstall(ActionDialogComp);

export default {
  ActionDialog,
};
